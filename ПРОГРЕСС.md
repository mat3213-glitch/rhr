# RHR — Прогресс сессии 07.07.26

## Аудит-ревью — завершено
Проведён полный аудит кодовой базы (audit-findings-2026-07-05.md). Найдено 49+ находок. Все исправлены.

## Исправления по аудиту (07.07.26)

### 🔴 Критические (безопасность)
| # | Проблема | Фикс | Файл |
|---|----------|------|------|
| F1 | XSS в landing page | `html.escape()` + cap длины | sandbox/demand_check.py |
| F2 | API ключи в .env | Создан .env.example, .gitignore обновлён | .env.example |
| F3 | Race condition в track | Single-flight через UPDATE WHERE NULL | pipeline/track.py |
| F4 | Prompt injection | `<input>` маркеры + system инструкция | pipeline/classify.py |
| F7 | Нет CHECK constraints | Добавлены CHECK на все enum-поля | data/schema.sql |
| F9 | Нет SQLite PRAGMAs | busy_timeout, synchronous, cache_size, temp_store | config_loader.py |
| F16 | XSS через `</script>` | `_safe_json()` экранирует `</` | dashboard/build.py |

### 🟠 Надёжность
| # | Проблема | Фикс | Файл |
|---|----------|------|------|
| F5 | Нет retry в LLM | 3 попытки с exponential backoff + returncode check | pipeline/classify.py |
| F8 | SSRF через redirects | `SafeRedirectTransport` + blocked hosts | collectors/http_util.py |
| F12 | `errorerror=error` опечатка | Исправлено на `error=` | pipeline/observability.py |
| F13 | Scoring defaults штрафуют | Unknown → нейтральные (1.0/0.0) | scoring/model.py |
| F14 | `trend_velocity DEFAULT 0.0` | Изменено на `0.5` | data/schema.sql |
| F15 | CVE в httpx/pydantic | `>=0.27.2,<1`, `>=2.6.4,<3` | requirements.txt |
| F16 | `_print_top` ломает end_run | Обёрнуто в try/except | run.py |
| R1 | Нет retry в коллекторах | Общий `http_util.retry` декоратор | collectors/http_util.py |
| R2 | Нет connect timeout | `connect=5.0` в общем клиенте | collectors/http_util.py |
| R3 | HN sequential 240 GET | ThreadPoolExecutor(max_workers=10) | collectors/hackernews.py |
| R4 | Telegram regex fragility | selectolax CSS парсинг | collectors/telegram.py |
| R5 | `cancel-in-progress: false` | Общий `group: db-write` для всех DB-workflows | .github/workflows/*.yml |
| R7 | YouTube quota exceeded | Интервал */12 + quotaExceeded детект | config/sources.yaml, collectors/youtube_search.py |

### 🟡 Гигиена
| # | Проблема | Фикс | Файл |
|---|----------|------|------|
| H7 | Опечатка в observability | Исправлено | pipeline/observability.py |
| H15 | Dead RSS feeds | Заменены на рабочие URL | config/sources.yaml |
| H20 | tests.yml без paths/concurrency | Добавлены paths-ignore, concurrency, timeout | .github/workflows/tests.yml |
| — | WAL checkpoint перед commit | `PRAGMA wal_checkpoint(TRUNCATE)` | workflow-файлы |
| — | `.env` парсер кавычки | `strip("\"'")` | run.py |
| — | `graduate_candidate(0)` | Валидация `--candidate` обязателен | run.py |
| — | `lru_cache` ломал rescore | `clear_weights_cache()` + `clear_yaml_cache()` | scoring/model.py, config_loader.py |
| — | `feedparser.parse()` timeout | httpx + parse(content) | collectors/rss_generic.py |
| — | `dedup_signals` память | Single UPDATE с subquery | pipeline/dedup.py |
| — | Dashboard stage_bars XSS | `_esc()` на funnel_stage | dashboard/build.py |
| — | Chart.js без SRI | Добавлен `integrity` + `crossorigin` | dashboard/index.html |
| — | Нет индексов | `CREATE INDEX` на run_log, scoring_feedback | data/schema.sql |
| — | MD5 для source IDs | SHA256[:16] в 6 файлах | collectors/*.py, pipeline/dedup.py |
| — | `_unix_to_iso(0)` → None | `if ts is None` | collectors/hackernews.py |
| — | grok-x-search pipefail | `bash -o pipefail -c "..."` | .github/workflows/grok-x-search.yml |

## Тесты: 356 pass
```
python3 -m pytest tests/ -q
356 passed in 9.47s
```

## Изменённые файлы (сессия 07.07.26)
- `sandbox/demand_check.py` — XSS фикс
- `pipeline/classify.py` — prompt injection + retry + enum validation + NaN protection
- `pipeline/track.py` — race condition fix (single-flight)
- `pipeline/observability.py` — typo fix
- `pipeline/score.py` — clear_weights_cache()
- `pipeline/dedup.py` — batch processing (single UPDATE)
- `config_loader.py` — SQLite PRAGMAs + clear_yaml_cache()
- `data/schema.sql` — CHECK constraints + trend_velocity DEFAULT + indexes
- `scoring/model.py` — NaN protection + neutral defaults + clear_weights_cache()
- `dashboard/build.py` — XSS via `</script>` + stage_bars _esc() + db_path param
- `dashboard/index.html` — Chart.js SRI
- `run.py` — _print_top try/except + .env quotes + graduate_candidate validation
- `requirements.txt` — CVE versions + upper bounds
- `collectors/http_util.py` — NEW: SSRF protection + retry + shared client
- `collectors/*.py` — все 8 коллекторов обновлены на общий клиент
- `collectors/hackernews.py` — ThreadPoolExecutor для concurrent HTTP + unix_to_iso fix
- `collectors/telegram.py` — selectolax вместо regex + SHA256
- `collectors/rss_generic.py` — httpx timeout вместо feedparser.parse()
- `collectors/youtube_search.py` — quotaExceeded детект
- `.github/workflows/*.yml` — cancel-in-progress, wal_checkpoint, paths filter, pipefail, shared concurrency
- `config/sources.yaml` — dead RSS replaced, YouTube interval */12
- `.env.example` — NEW
- `.gitignore` — .env.example exception
- `tests/test_http_util.py` — NEW: 19 тестов SSRF + retry
- `tests/test_run.py` — NEW: 11 тестов CLI команд
- `tests/test_collectors_phase2.py` — mock paths updated
- `tests/test_reddit.py` — mock paths updated
- `tests/test_telegram.py` — mock paths updated (httpx → http_client)
- `tests/test_telegram_integration.py` — mock paths updated
- `tests/test_sandbox.py` — CHECK constraint compatible + update_metrics + list_mvps tests
- `tests/test_dedup.py` — SHA256 key length
- `tests/test_hackernews.py` — unix_to_iso(0) fix
- `tests/test_rss.py` — SHA256 key length
- `tests/test_normalize.py` — SHA256 key length
- `tests/test_observability.py` — test_log_metric assertions
- `tests/test_dashboard.py` — temp DB instead of real

## Следующие шаги
- [ ] Перенести API ключи из .env в GitHub Secrets (YOUTUBE_API_KEY, PRODUCTHUNT_API_TOKEN)
- [ ] Добавить XAI_API_KEY в GitHub Secrets (для Grok)
- [ ] Telegram collector (нужен self-hosted runner)
