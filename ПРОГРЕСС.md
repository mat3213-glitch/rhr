# RHR — Прогресс сессии 28.06.26

## Phase 2 Sources — завершено
| Коллектор | Статус | Файл |
|-----------|--------|------|
| defillama | ✅ Включён (v2/chains endpoint) | collectors/defillama.py |
| producthunt | ✅ Включён (GraphQL API) | collectors/producthunt.py |
| youtube | ✅ Включён | collectors/youtube_search.py |
| gumroad | ✅ Включён (fixed regex) | collectors/gumroad.py |
| perplexity | ❌ Удалён | — |
| x_via_grok | ⏳ Ожидает XAI_API_KEY | scripts/grok_search.py |

## Phase 3 Sources — завершено
| Коллектор | Статус | Файл |
|-----------|--------|------|
| reddit | ✅ Включён (old.reddit.com RSS) | collectors/reddit.py |
| telegram | ⏳ Ожидает API key + runner | — |

## LLM Classification — подключён
| Параметр | Значение |
|----------|----------|
| Backend | GitHub Models (GPT-4o-mini) |
| Endpoint | https://models.inference.ai.azure.com |
| Auth | gh auth token (auto) |
| Статус | ✅ Работает, бесплатно |

## Product Hunt API — подключён
| Параметр | Значение |
|----------|----------|
| Token | MFUi1MVkr7... (в .env) |
| Endpoint | GraphQL API v2 |
| Статус | ✅ Работает |

## CLI команды
```
python3 run.py init              # применить схему
python3 run.py scan              # все источники
python3 run.py scan hackernews   # конкретный источник
python3 run.py pipeline          # dedup + classify + score
python3 run.py status            # воронка
python3 run.py dashboard         # генерация dashboard
python3 run.py demand-check --candidate ID
python3 run.py mvp --candidate ID
python3 run.py verdict RUN_ID go|no_go
python3 run.py metrics --days 7
python3 run.py ingest-stdin      # импорт JSON lines
```

## API ключи (.env)
- `YOUTUBE_API_KEY` — включён
- `PRODUCTHUNT_API_TOKEN` — включён
- `RHR_LLM_BASE_URL` — GitHub Models (auto)
- `XAI_API_KEY` — нужен для Grok (добавить в GitHub Secrets)

## Тесты: 326 pass
- Phase 2: 20 tests (test_collectors_phase2.py)
- Reddit: 7 tests (test_reddit.py)
- Всего: 326/326 pass

## Исправления в этой сессии
1. **Gumroad** — исправлен regex (gumroad.com/l/[slug] вместо /product/)
2. **Reddit** — переключён на old.reddit.com RSS (redlib зеркала мертвы)
3. **DeFiLlama** — переключён на /v2/chains (/protocols слишком большой)
4. **Product Hunt** — добавлен GraphQL API + логирование
5. **LLM classify** — подключён через GitHub Models (GPT-4o-mini)
6. **Создано** scripts/grok_task.md — задание для Grok scanner

## Следующие шаги
- Telegram collector (нужен TELEGRAM_API_ID + TELEGRAM_API_HASH + self-hosted runner)
- Grok: добавить XAI_API_KEY в GitHub Secrets
- Запустить полный скан с новыми источниками
